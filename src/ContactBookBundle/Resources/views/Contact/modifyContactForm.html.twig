{% extends "::base.html.twig" %}

{% block title %}Modify Contact{% endblock %}
{% block stylesheets %}
    {{ parent() }}
    <link href="{{ asset('bundles/contactbook/css/modifyForm.css') }}" rel="stylesheet">
{% endblock %}

{% block body %}
     <div class="container">
      <h2>Modify contact:</h2>
    
        {{ form_start(form) }}
        {{ form_widget(form) }}
        {{ form_end(form) }}
        
     </div>
     <div class="container2">
       <p>
         <button type="button" class="btn btn-default">
           <a href="{{ path('getContact', {'id': id}) }}">Back</a>
         </button> 
       </p>
     </div>
{% endblock %}




